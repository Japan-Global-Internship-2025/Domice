import styled from "styled-components";

const Container = styled.div`
    overflow-y: auto; 
    padding-bottom: 60px;
`;

export default function HomeOut() {
    return (
        <Container>
            외출관리
        </Container>
    )
}